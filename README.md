# company-github-page

Tutorial:

Install the [gh-pages app](https://github.com/gitname/react-gh-pages)
```shell
$ npm install gh-pages --save-dev
```

Then run with
```shell
$ npm start
```

Deploy with
```shell
$ npm run deploy
```
